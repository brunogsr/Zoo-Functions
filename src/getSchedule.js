// const { hours } = require('../data/zoo_data');
const data = require('../data/zoo_data');

const getSchedule = (scheduleTarget) => {};
//   const weekDays = data.hours;
//   if (!scheduleTarget) {
//     const noParameter = Object.entries(weekDays).reduce((accumulator, [day, { open, close }]) => {
//       const message = `Open from ${open}am until ${close}pm`;
//       accumulator[day] = message;
//       return accumulator;
//     }, {});
//     return noParameter;
//   }
// };

// console.log(getSchedule());

module.exports = getSchedule;
//  officeHour: `Open from ${} until ${}`, exhibition: []

//  console.log(weekDays);
// [
//   'Tuesday',
//   'Wednesday',
//   'Thursday',
//   'Friday',
//   'Saturday',
//   'Sunday',
//   'Monday'
// ]
